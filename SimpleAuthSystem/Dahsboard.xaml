<Window x:Class="SimpleAuthSystem.Dashboard"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Event Dashboard"
        Height="450"
        Width="800"
        WindowStartupLocation="CenterScreen"
        Background="#F5F6FA">

    <Grid Margin="20">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Title -->
        <TextBlock Text="Event Sales Dashboard"
                   FontSize="26"
                   FontWeight="Bold"
                   Margin="0,0,0,10"/>

        <!-- Refresh Button -->
        <Button Grid.Row="1"
                Content="Refresh"
                Width="100"
                Height="30"
                HorizontalAlignment="Right"
                Margin="0,0,0,10"
                Click="RefreshButton_Click"/>

        <!-- Table -->
        <Border Grid.Row="2"
                Background="White"
                CornerRadius="10"
                Padding="10">

            <DataGrid Name="EventTable"
                      AutoGenerateColumns="False"
                      CanUserAddRows="False"
                      RowHeight="35">

                <DataGrid.Columns>
                    <DataGridTextColumn Header="Event"
                                        Binding="{Binding EventName}"
                                        Width="*"/>

                    <DataGridTextColumn Header="Tickets Sold"
                                        Binding="{Binding TicketsSold}"
                                        Width="150"/>

                    <DataGridTextColumn Header="Total Profit"
                                        Binding="{Binding TotalProfit}"
                                        Width="150"/>
                </DataGrid.Columns>

            </DataGrid>
        </Border>

    </Grid>
</Window>